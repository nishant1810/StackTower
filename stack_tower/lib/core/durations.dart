class AppDurations {
  AppDurations._();

  static const splash = Duration(seconds: 3);

  static const fade = Duration(milliseconds: 1200);

  static const zoom = Duration(seconds: 4);

  static const glow = Duration(milliseconds: 1800);

  static const floating = Duration(seconds: 2);

  static const loading = Duration(seconds: 3);

  static const transition = Duration(milliseconds: 800);
}